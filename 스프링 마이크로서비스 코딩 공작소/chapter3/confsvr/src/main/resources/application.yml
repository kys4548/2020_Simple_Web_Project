server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kys4548/microservice-config-repo
          search-paths: licensingservice,organizationservice